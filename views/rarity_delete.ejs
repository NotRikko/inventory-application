<!DOCTYPE html>
<html>
  <head>
    <title>TFT Inventory</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <% include navbar %>
    <div class="main">
        <h1>Are you sure you want to delete this rarity?</h1>
        <h1><%= rarity.name %></h1>
        <p><%= rarity.cost %></p>
        <p>Please delete the following units from this rarity.</p>
        <h4>Units</h4>
            <% allUnitsInRarity.forEach((unit) => { %>
                <div>
                    <img src="<%= unit.image %>">
                    <a href="<%= unit.url %>"><%= unit.name %></a>
                </div>
            <% }) %>
        <form method="POST">
            <button type="submit">Delete</button>
        </form>
    </div>
  </body>
</html>
